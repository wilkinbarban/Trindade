<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest overlay: it attaches the network security config that permits cleartext to the
  emulator loopback. Living under src/debug/ is what keeps the exemption out of release builds, so
  this file is the mechanism. The policy itself, including why the exemption is limited to the
  loopback, lives in src/debug/res/xml/network_security_config.xml.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
